{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <header>
        <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse"
                            data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand {% if app.request.attributes.get('_route') == 'main_homepage' %}active{% endif %}" href="{{ path('main_homepage') }}">Shoefony</a>
                </div>
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li>
                            <a class="{% if app.request.attributes.get('_route') == 'main_presentation' %}active{% endif %}" href="{{ path('main_presentation') }}">Présentation</a>
                        </li>
                        <li>
                            <a class="{% if app.request.attributes.get('_route') starts with 'store_' %}active{% endif %}" href="{{ path('store_list_product') }}">Nos produits</a>
                        </li>
                        <li>
                            <a class="{% if app.request.attributes.get('_route') == 'main_contact' %}active{% endif %}" href="{{ path('main_contact') }}">Contact</a>
                        </li>
                    </ul>

                    <form class="navbar-form navbar-right">
                        <div class="form-group">
                            <input type="text" placeholder="Recherche" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-success">OK</button>
                    </form>
                </div>
            </div>
        </nav>
    </header>
{% block content %}
{% endblock %}
    <footer>
        <p>Copyright &copy; Shoefony 2019  <b>{{ "now"|date('d/m/Y') }}</b></p>
    </footer>
{% endblock %}